server:
  port: 8761

spring:
  application:
    name: 'server'

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://localhost:8761/eureka
